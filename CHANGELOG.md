# Changelog

## Unreleased

### Added
- Connectivity retry logic with Client AP reconnect, 10s/20s waits, and optional manual down-arrow retry prompt.
- Configurable reconnect command and retry settings in `payload.sh`.
